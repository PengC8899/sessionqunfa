# Telegram 群发控制面板使用说明

本文档详细介绍了如何使用 Telegram 群发控制面板（基于 Anna VPS `3.34.232.231` 环境编写）。

## 1. 访问与登录

- **访问地址**: [http://3.34.232.231:8000](http://3.34.232.231:8000)
- **管理员令牌 (Admin Token)**: `999999`
  - *提示：首次打开页面或令牌过期时，需要在页面顶部的 "Admin Token" 输入框中填入此密码并点击 "Save"。*

## 2. 账号登录与管理

面板支持多账号管理，所有账号的会话文件存储在服务器的 `/home/ubuntu/qunfa/sessions/` 目录下。

### 首次登录账号
1. 在页面左上角的 **"Select Account"** 下拉菜单中选择一个账号（例如 `account_01`）。
2. 如果该账号显示 **🔴 Unauthorized**（未授权）：
   - 在 **"Phone Number"** 输入框中填写手机号（格式如 `+8613800000000`）。
   - 点击 **"Send Code"**。
   - 在手机 Telegram App 上收到验证码后，填入 **"Code"** 输入框。
   - 点击 **"Sign In"**。
3. 登录成功后，状态将变为 **🟢 Authorized**。

### 批量切换
- 使用下拉菜单可以在不同账号间快速切换，查看各账号的群组列表和状态。

## 3. 核心功能

### 3.1 自动加群 (Auto Join)
用于批量加入 Telegram 群组文件夹（Chat Folders）。

1. 找到页面中部的 **"自动加入群组链接 (Auto Join Chat Folder URLs)"** 区域。
2. 在文本框中粘贴 `t.me/addlist/...` 链接（一行一个）。
3. 点击 **"Join Folders"** 按钮。
4. 系统将自动控制当前选中的账号加入这些文件夹中的所有群组。

### 3.2 消息群发 (Bulk Send)
这是系统的核心功能，用于向已加入的群组发送消息。

**步骤：**
1. **选择群组**:
   - 在左侧 **"Groups / Channels"** 列表中，勾选要发送的目标。
   - 可以使用搜索框筛选，或点击 **"Select All"** 全选。
2. **编写消息**:
   - 在右侧 **"Message Text"** 区域输入要发送的内容。
   - 支持普通文本或 Markdown 格式。
3. **设置参数 (可选)**:
   - **Parse Mode**: 建议选 `None` (纯文本) 或 `Markdown`。
   - **Delay (ms)**: 每条消息之间的间隔（毫秒），建议保持默认或设为 `1500` 以防封号。
4. **开始发送**:
   - 点击 **"Send to Selected"** 按钮。
   - 任务将在后台异步运行，页面下方的 **"Task Logs"** 会实时显示发送进度和结果。

## 4. 系统配置与限制

当前 VPS (`3.34.232.231`) 的关键配置参数如下：

- **并发限制**: `GLOBAL_SEND_CONCURRENCY = 30`
  - *含义*：系统最多同时允许 30 个账号进行发送任务。
- **自动回复**: **已禁用**
  - *说明*：该 VPS 上的代码已移除私聊自动回复功能，账号收到私信不会有任何自动响应。

## 5. 常见问题与维护

### Q: 打开页面显示 "Unauthorized" 或无法加载群组？
**A**: 可能是会话文件过期或被 Telegram 登出。
- **解决方法**: 在面板上重新输入手机号和验证码进行登录。

### Q: 想要清空所有账号重新登录？
**A**: 需要在服务器后台执行命令清空 `sessions` 目录（已在之前操作中演示过），然后重启服务。

### Q: 如何重启服务？
**A**: 通过 SSH 登录服务器后执行：
```bash
cd /home/ubuntu/qunfa
docker compose restart web
```

### Q: 为什么发送速度变慢？
**A**: 为了防止账号被 Telegram 封禁，系统内置了限流机制（Delay）。请勿将间隔设置得过短（建议 > 1000ms）。
